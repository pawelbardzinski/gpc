<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
	<meta http-equiv="Content-Language" content="pl">
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
	
	<title>Gold Chart & News</title>    
	<%= stylesheet_link_tag "style.css" %>
	<%= javascript_include_tag "jquery" %>
	<%= javascript_include_tag "script" %>
	<%= csrf_meta_tags %>
</head>
<body>
	<div class="wrap">
		<div class="head">
			<div class="logo">&nbsp;</div>
			<div class="info">
			<% if session[:login] != nil %>
				<div class="user">You are logged in as <span class="username"><%= session[:login] %></span></div>
				<span class="points">
					<%= p = User.find(session[:user_id]).karma %>
					<% if p == 1 %>point<% else %>points<% end %>
				</span>
			<% end %>
			</div>
			<div class="navigation">
				<a href="/" class="home">Home</a>
				<a href="/" class="news">News</a>

				<% if session[:login] == nil %>
					<%= link_to "Login",{:controller=>'topics',:action=>'login'}, :class => 'login dialog', :rel=>"dialog_content" %>
					<%= link_to "Register",{:controller=>'topics',:action=>'login'}, :class => 'register dialog', :rel=>"dialog_content" %>
				<% else %>
					<%= link_to "Logout",{:controller=>'topics',:action=>'logout'}, :class => 'logout', :rel=>"dialog_content" %>
				<% end %>
			</div>
			<div class="clear"></div>
		</div>

		<iframe src="http://goldpricecafe.com:8400/alive5/Alive6_HiRes/Alive6.html?wmode=transparent" style="width:960px;height: 800px;" frameborder="0" allowfullscreen></iframe>

		<%= yield %>
	</div>
	<div class="foot">
		<div class="in">
			<div class="logo">&nbsp;</div>
			<div class="navigation">
				<a href="/" class="home">Home</a>
				<a href="/" class="news">News</a>
			</div>
		</div>
	</div>
	<div id="dialog_overall"></div>
	<div id="dialog"><div id="dialog_content"></div></div>
</body>
</html>
